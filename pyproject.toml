[project]
name = "aids"
version = "0.1.0"
description = "AIDS 企业级电商平台 + AI 智能客服"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    # gunicorn + uvicorn worker：deploy/app/*.Dockerfile 的 CMD 依赖它。
    # 曾经"CMD 里用了、依赖里没声明"——镜像能起来，运行时 executable not found。
    "gunicorn>=23.0",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "sqlalchemy[asyncio]>=2.0.36",
    "alembic>=1.14",
    "asyncmy>=0.2.9",
    "redis>=5.2",
    "aiokafka>=0.12",
    "pyjwt[crypto]>=2.10",
    "cryptography>=44.0",
    # 密码哈希（替代 passlib，见 docs/项目设计报告.md 附录 A）
    "bcrypt>=4.2",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "hypothesis>=6.120",
    # 反射层契约测试（C1 requires_mysql）用 tests/conftest.py 的
    # _to_sync_url() 把 mysql+asyncmy:// 转成 mysql+pymysql://。
    # 曾经只在 CI 里单独 pip install——本地按 README 装完仍连不上库。
    "pymysql>=1.1",
    "ruff>=0.8",
    "pyright>=1.1.390",
    "pre-commit>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

# ---------------------------------------------------------------------
# pytest —— tests/contract 与 tests/invariants 是 CI 门禁核心
# ---------------------------------------------------------------------
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
# 服务包 aids_backend 位于 aids-backend/ 下（目录名带连字符，不是合法模块名），
# 因此不在仓库根，也不在 site-packages。显式加进来，测试里才能
# `from aids_backend.app_factory import create_app`。
# 共享层 app/ 在仓库根，已由 rootdir 自动入 path，无需声明。
# 新增服务（aids_ai / aids_mock）时在此追加。
pythonpath = ["aids-backend"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-ra",
    "--strict-markers",
    "--tb=short",
]
markers = [
    "contract: 文档 ↔ 代码 一致性测试（CI 必过，门禁核心）",
    "invariant: 业务不变量测试（CI 必过）",
    "requires_mysql: 需要真实 MySQL 8 实例（CI 提供 services）",
    "slow: 慢测试",
]

# ---------------------------------------------------------------------
# ruff —— 格式与静态检查
# ---------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["deploy", "docs", ".trae"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
]
ignore = [
    "E501",  # 行长度由 formatter 处理
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["B011"]

[tool.ruff.lint.isort]
# 为什么要显式声明：ruff 自动识别"第一方包"靠的是**目录名与模块名一致**，
# 而服务目录是 `aids-backend`（连字符）→ 模块名 `aids_backend` 匹配不上，
# 会被归入第三方组，import 顺序与 app.* 分家（I001）。
# 显式列出后：stdlib | 第三方 | 第一方(app / aids_* / tests)，语义清晰。
known-first-party = ["app", "aids_backend", "aids_ai", "aids_mock", "tests"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

# ---------------------------------------------------------------------
# pyright —— 类型检查（标准档：basic）
# ---------------------------------------------------------------------
[tool.pyright]
include = ["app", "tests", "aids-backend/aids_backend"]
exclude = ["**/node_modules", "deploy", "docs"]
# aids_backend 不在 site-packages：它是源码目录 aids-backend/ 下的包，
# 需显式加入解析路径，否则整个服务层都是 reportMissingImports。
# CI 的 typecheck job 动态生成 pyrightconfig.json，同样要带上这个路径。
extraPaths = ["aids-backend"]
pythonVersion = "3.11"
typeCheckingMode = "basic"
reportMissingImports = true
reportMissingTypeStubs = false
# 类型检查必须用仓库自己的 venv：tests/ 里 import pytest / hypothesis，
# 若 pyright 解析到系统解释器（没装这些包）会报 22 处 reportMissingImports
# ——"类型检查失败"变成"环境没配好"，真假问题混在一起。
venvPath = "."
venv = ".venv"
